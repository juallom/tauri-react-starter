{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "//#tauri-dev": {
      "persistent": true
    },
    "//#tauri-build": {
      "dependsOn": ["webview#build"]
    },
    "//#lint": {},
    "//#lint:fix": {}
  }
}
